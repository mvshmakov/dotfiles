# This file affects settings that affect all programs that use the GNU readline library

$include /etc/inputrc

# Do what "set -o vi" does in ZSH (vim editing mode) to whatever uses readline.
set editing-mode vi
